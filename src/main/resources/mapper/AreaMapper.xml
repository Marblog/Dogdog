<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.me.dogdog.mapper.AreaMapper">
    <select id="selectByPage" resultType="com.me.dogdog.entity.Area">
        select  *
        from china_area.area limit #{limit} ,#{pageSize};
    </select>
</mapper>